{
  "name": "visibility-audit-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute visibility-audits --file=sql/schema.sql"
  }
}
